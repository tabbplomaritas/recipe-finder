"use strict";

const favoritesPage = {
  template: `

  `,
  controller: ["RecipeRequest", function(RecipeRequest) {
    const vm = this;

  }]
};

angular
  .module("App")
  .component("favoritesPage", favoritesPage);